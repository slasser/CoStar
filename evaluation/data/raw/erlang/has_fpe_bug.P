-file("test/erts/emulator/test/float_SUITE_data/has_fpe_bug.erl", 1).

-module(has_fpe_bug).

-export([start/0]).

start() ->
    case catch math:log(- 1.0) of
        {'EXIT',{badarith,_}} ->
            halt(0);
        _ ->
            file:write_file(skip_reason, "Known FPE bug"),
            halt(1)
    end.



